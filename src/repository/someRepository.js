//
//      Repository
//

/**
 * Save
 * 
 * @param param
 */
export const save = async (param) => {

    try {
        // save
    } catch (error) {
        console.error(error)
    }
}

/**
 * Load
 * 
 * @param param
 */
export const load = async (param) => {

    try {
        // save
    } catch (error) {
        console.error(error)
    }
}
